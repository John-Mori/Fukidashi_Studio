# Fukidashi Studio 設計書 v1.0

| メタ情報 | 内容 |
|---|---|
| 案件名 | Fukidashi Studio（仮称） |
| 作成日 / 版 | 2026-07-09 / v1.3 |
| 実行エージェント | Codex |
| 対象環境 | Windows 11 デスクトップを先行。将来 Android / iOS 対応を想定 |
| 規模プロファイル | M（中） |
| 主目的 | 読み込んだ画像へ、ユーザー所有の吹き出しテンプレ画像・テキスト・図形をGUI操作で合成・編集・書き出す。主対象は縦長の漫画画像だが、852×1280px を含む複数サイズへ柔軟対応する |

> この文書は、そのまま Codex に渡して実装させるための設計書である。
> 第一段階では Windows PC での実用性を最優先する。ただし、編集ロジック・UI・データモデルを Tauri 固有APIから分離し、後から Android / iOS へ移植しやすい構成にする。
>
> **最重要方針:** 「画像編集ソフトを作る」のではなく、**吹き出し合成に特化した高速ワークフロー**を作る。Photoshopの代替を目指さない。

---

# Part I ─ 共通ルール

## R1. 役割分担と進め方

この作業は「設計・判断」と「実装の物量」を分けて進めること。

- 上位の役割: 設計書の解釈、方式選択、UI/UXの統一、成果物レビュー、統合、受け入れ判定。
- 実装側の役割: 個別機能、テスト、ファイル生成、検証、ログ収集。
- Codex にサブタスク分割機能がある場合は、UI、Canvas、Tauri I/O、テストを分けてよい。
- 同一ファイルを複数担当が同時編集しない。

### 着手フロー

1. この設計書を通読する。
2. 環境依存の不明点だけをまとめる。
3. 5章のチェックポイント表をベースに短い実装計画を作る。
4. Chami の承認、または「任せる」の指示後に実装へ入る。
5. 承認済み範囲内は自走する。

### 委譲チケット形式

```text
【委譲チケット】
- 目的: 何のための作業か
- 入力: 対象ファイル、前提、既存仕様
- 出力: 生成物と配置先
- 受け入れ条件: 完了判定。可能なら検証コマンドを含める
```

---

## R2. 質問・エスカレーション・ストッパー

以下は勝手に決めず Chami に確認する。

- 既存ファイルの削除・上書き・初期化
- git の `reset --hard`、`push --force`、`clean -f`、ブランチ削除
- 外部公開、ポート開放、`0.0.0.0` バインド
- 課金サービスの利用
- クラウドへの画像送信
- 要件を変える仕様変更
- ソース画像を不可逆に変更する処理

瑣末なUI値は合理的な既定値で進めてよい。採用値は `STATUS.md` の決定事項ログに残す。

同じ失敗を3回程度繰り返す、原因不明のビルド失敗が連続する、依存関係の差し替えが必要になる場合は、無限試行せず状況を整理する。

---

## R3. セキュリティ共通原則

このアプリは原則ローカル完結とする。

- 外部APIはMVPでは使用しない。
- 画像・テンプレ・プロジェクト内容を外部へ送信しない。
- CDN依存は禁止。CSS、JS、フォント等を外部URLから読み込まない。
- Tauri の権限は最小化し、必要な dialog / fs / path 相当だけを有効化する。
- shell 実行権限はMVPでは付与しない。
- ファイルアクセスはアプリデータ領域と、ユーザーが選択したファイル/保存先に限定する。
- CSPを設定する。
- ログに画像データ本体、Data URL、個人情報を出さない。
- ユーザーが読み込んだSVGは潜在的に危険な入力として扱う。SVGテンプレ対応を行う場合、外部参照、スクリプト、イベント属性を無効化・サニタイズする。安全化が難しければMVPではPNG/WebPのみ対応としてよい。
- APIキー・トークン等は現時点で不要。将来必要になった場合は `.env` と `.env.example` を分ける。
- `.env`、ビルド成果物、ローカルプロジェクトデータは `.gitignore` へ追加する。

---

## R4. 記録と再開

プロジェクト直下に `STATUS.md` を置く。

```markdown
# STATUS ─ Fukidashi Studio

## 完了
- [x] CP1: ...

## 進行中
- [ ] CP2: ...

## 未着手
- [ ] CP3: ...

## 次の一手
- 再開時に最初に行うこと

## 決定事項ログ
- YYYY-MM-DD: 決定内容（理由: ...）
```

- git を使用する。
- CPごとに動作確認後、`STATUS.md` を更新してコミットする。
- コミットは意味のある単位で行う。
- 中断前に「次の一手」を必ず更新する。

---

# Part II ─ 案件仕様

## 1. 目的と要件

## 1-A. v1.2追加要件（Chami追加要望反映）

## 1-AA. v1.3追加要件（可変画像サイズ対応）

この節は、画像サイズ固定前提を避けるための追加仕様であり、**優先度は高**とする。

### 基本方針

- 本アプリは **特定1サイズ専用** にしない。
- 主用途は縦長漫画画像だが、サイズが毎回異なる前提で設計する。
- 特に以下のようなサイズを自然に扱えること。
  - 852 × 1280 px
  - 1022 × 1536 px
  - 1122 × 1402 px
  - その他の縦長比率画像
- 画像サイズごとにUIや保存処理が破綻しないこと。

### キャンバス仕様

- **内部論理キャンバスサイズは、読み込んだ元画像サイズに一致**させる。
- 画面表示は常にビューポート上でスケーリングし、元解像度そのものを変更しない。
- ズーム、Fit表示、パンは表示用変換として扱う。
- 書き出し時は必ず元画像ピクセル寸法を維持する。
- 画像差し替えや新規読み込み時は、前の画像サイズ前提のUI状態を引きずらないこと。

### サイズ依存ロジックの禁止

以下を禁止する。

- キャンバス寸法を 852 × 1280 や 1022 × 1536 へ固定する実装
- オブジェクト位置やサイズの保存を「画面上の見た目ピクセル」で持つ実装
- 書き出し時に表示倍率をそのまま解像度へ反映する実装
- 特定画像サイズだけで見た目が成立する余白、固定座標、固定スケール前提

### UI要件

- 縦長画像を基本としつつ、各サイズで**全体表示(Fit)** が自然に効くこと。
- 右プレビュー領域は、画像サイズが異なっても視認しやすいように拡大縮小されること。
- ステータスバーに以下を表示する。
  - 元画像サイズ
  - 現在の表示倍率
  - 選択中オブジェクト種別
- 新規プロジェクト開始時や画像読込後、現在サイズを明示する。
- 画像サイズが異なっても吹き出し・文字・図形の操作感が破綻しないこと。

### プリセットと将来拡張

- MVPでは「任意画像読込ベース」を基本にする。
- 補助機能として、任意に**新規キャンバスのサイズプリセット**を用意してよい。
- 推奨プリセット例:
  - 852 × 1280
  - 1022 × 1536
  - 1122 × 1402
- ただし、プリセットは補助であり、設計の本質は**任意サイズ対応**に置く。

### データモデル方針

- ProjectDocument の `canvas.width` / `canvas.height` は画像ごとに可変。
- オブジェクト座標は論理キャンバス基準で保存する。
- 必要に応じて、将来的には「相対座標保存」または「別サイズへのレイアウト再適用」を拡張可能な構造にする。
- ただしMVPでは、まず「読み込んだ画像上で正しく編集・保存できる」ことを優先する。

### テスト要件

最低限、以下のサイズで手動または自動テストする。

1. 852 × 1280
2. 1022 × 1536
3. 1122 × 1402
4. それ以外の任意縦長画像 1種

確認項目:

- Fit表示
- ズーム / パン
- 吹き出し追加
- 文字追加（縦 / 横）
- 図形追加
- スポイト
- PNG書き出し
- 書き出し寸法一致
- 自動保存 / 復旧



この節は、v1.1に対する追加仕様であり、**優先度は高**とする。

### UIレイアウト方針

- **標準レイアウト**
  - 左: 操作タブ / テキスト編集 / 素材 / 履歴
  - 右: 画像プレビュー / 編集キャンバス
- **設定で左右反転可能**
  - `layout.previewPosition = "right" | "left"`
  - 初期値は `"right"`
- **下部プロパティエリア**
  - 吹き出し、文字、図形の詳細調整パネルを下部に配置できる。
  - 少なくとも「文字内容」「文字サイズ」「吹き出しサイズ」「色」変更時は**即時プレビュー反映**する。
  - 実装上は、下部固定パネルまたは左パネル内の下部ドッキング領域のいずれでもよいが、見た目として「下部で細かく調整 → 右プレビューで即確認」が成立すること。

### テキスト編集追加仕様

- 文字方向は **縦 / 横 切替** に対応する。
- **標準は縦書き** とする。
- 吹き出し用途に合わせて、テキスト追加時の既定値は以下。
  - 文字色: 黒
  - 縁取り: ON
  - 縁取り色: 白または黒から選択可能な既定値（MVPでは白を推奨）
- 変更項目:
  - 文字内容
  - 縦書き / 横書き
  - フォント
  - 文字サイズ
  - 文字色
  - 文字縁取りの有無
  - 縁取り色
  - 縁取り太さ
  - 行間
  - 字間
  - 揃え
- 変更時はプレビューへ即時反映する。
- 縦書きは日本語漫画で使える品質を目標とする。
  - MVPでは「実用的な縦組み」が成立することを優先する。
  - 厳密な禁則や特殊約物処理は改善項目として段階実装でもよい。

### 図形ツール追加仕様

- 基本図形:
  - 四角形
  - 角丸四角形
  - 円 / 楕円
  - 直線（任意。MVPに入れてよい）
- 各図形で変更可能な項目:
  - 塗り色
  - 線色
  - 線幅
  - 不透明度
  - 回転
  - レイヤー順
- 図形作成後の再編集を可能にする。
- 図形色は通常のカラーピッカーに加えて、**スポイトで画像から色を取得**できること。

### スポイト機能追加仕様

- ベース画像またはキャンバス上の表示色を取得できる。
- 取得した色は次の用途に流用できる。
  - 図形の塗り色
  - 図形の線色
  - 文字色
  - 文字縁取り色
- 最低限、ユーザーの操作フローは以下を満たす。
  1. スポイトを選ぶ
  2. プレビュー上の任意箇所をクリック
  3. 色が取得される
  4. 現在選択中オブジェクトへ反映、またはカラースウォッチへ保存される

### 履歴・素材呼び出し追加仕様

- 履歴は二系統に分ける。
  1. **編集履歴**
     - Undo / Redo
     - 少なくとも50履歴
  2. **使用素材履歴**
     - 過去に使った吹き出しテンプレ
     - 過去に使った図形スタイル
     - 過去に使った文字スタイル
     - 過去に開いたプロジェクト
- 「過去に使った素材を呼べる」要件のため、**Recent Assets** 機能を追加する。
- Recent Assetsに表示する候補:
  - 最近使った吹き出しテンプレ
  - 最近使った色
  - 最近使ったテキストスタイル
  - 最近使った図形スタイル
- 将来拡張として **Favorite / Pin留め** も可能な構造にする。

### 画面構成の実装指針

主対象画像は、添付例のような**縦長漫画画像**であり、**1022×1536px や 852×1280px を含む複数サイズ**を前提とするため、以下を守る。

- キャンバス領域は縦長画像の全体確認に向く設計にする。
- 初期表示は「全体が収まるFit表示」を基本にする。
- 吹き出し作業では右プレビューを十分広く確保する。
- 左パネルはカテゴリ別タブ構成にする。
  - 例: `テキスト / 吹き出し / 図形 / 素材 / 履歴 / 設定`
- 下部プロパティには現在選択中オブジェクトに応じた内容のみを出す。
  - 文字選択時 → 文字項目
  - 吹き出し選択時 → サイズ・不透明度など
  - 図形選択時 → 塗り・線・角丸等

### 優先実装順（v1.2）

1. 左操作 / 右プレビュー / 左右反転設定
2. 下部プロパティ + 即時プレビュー
3. 文字の縦 / 横切替（標準は縦）
4. 文字縁取り
5. 図形ツール
6. スポイト
7. Recent Assets / 過去素材呼び出し
8. 文字スタイル・図形スタイルの再利用性向上



### 1-1. 要件定義

| 項目 | 内容 |
|---|---|
| やりたいこと | PC上で画像を読み込み、吹き出しテンプレ画像と文字をGUIで配置・編集し、合成画像を書き出す |
| 最優先 | 操作が直感的で、画像編集ソフトに慣れていなくても短時間で使えること |
| UI方針 | 古い業務ソフト風を避け、現代的なダークUIを基本にする |
| 動作環境 | 先行: Windows 11。将来: Android / iOS。macOSは構造上対応可能にしておくが初期検収対象外 |
| 成功の定義 | 画像読込→吹き出し配置→文字入力→字体/サイズ/色変更→移動/拡縮→PNG書出しがGUIだけで完了し、852×1280px を含む複数画像サイズでも同様に成立する |
| 検収方法 | Chami が任意画像と任意の透明PNG吹き出しテンプレを読み込み、テキストを調整し、元画像と同じピクセル寸法のPNGを正常に保存できること |
| 前提・制約 | ローカル完結。MVPではクラウド同期、AI生成、OCR、自動配置を行わない |
| 扱うデータ | ローカル画像。外部送信なし。アプリデータ削除でローカルキャッシュを消せること |

### 1-2. MVPの必須機能

#### A. ベース画像

- PNG / JPEG / WebP の読み込み。
- ドラッグ&ドロップ対応。
- ファイル選択ダイアログ対応。
- 読み込んだ画像の縦横比とピクセル寸法を保持。
- 特定サイズへ自動変換しない。
- 852×1280px を含む任意の縦長画像をそのまま扱える。
- 画面表示用の縮小率と書き出し解像度を分離する。
- キャンバス表示倍率を変えても書き出し画質を落とさない。
- 「画像を画面に合わせる」「100%表示」「拡大」「縮小」。
- 初期表示は画像全体が収まるFit表示を標準とする。
- Space + ドラッグ、または中ボタンでパン操作。

#### B. 吹き出しテンプレ

- ユーザーが用意した透明PNG / WebPをテンプレとして登録できる。
- テンプレライブラリにサムネイル表示する。
- サムネイルをクリックするとキャンバス中央へ追加。
- ドラッグ&ドロップでキャンバスへ直接追加可能。
- 移動。
- 拡大縮小。
- 回転。
- 左右反転。
- 上下反転。
- 不透明度変更。
- 縦横比ロックは既定でON。
- 前面へ、背面へ、1段上、1段下。
- 複製。
- 削除。
- ロック。
- 表示/非表示。

#### C. テキスト

- テキスト追加。
- 文字方向の切替（縦 / 横）。初期値は縦。
- キャンバス上でダブルクリックして編集開始。
- 右側インスペクターにテキスト入力欄を常設。
- 日本語IME入力で破綻しない。
- フォント変更。
- フォントサイズ変更。
- 文字色変更。既定値は黒。
- 文字の縁取り（ストローク）ON/OFF。
- 文字縁取り色変更。
- 文字縁取り太さ変更。
- 太字。
- 斜体。
- 文字揃え: 左 / 中央 / 右。
- 行間。
- 文字間隔。
- テキストボックス幅変更と自動折返し。
- テキストオブジェクトの移動、回転、複製、削除、ロック。
- 最低限のフォント候補をアプリ内プリセットとして持つ。
- 実際に利用可能なフォントだけを候補表示する仕組みを用意するか、利用不可時にフォールバックしていることをUIで分かるようにする。

**MVP推奨フォント候補:**

- BIZ UDPGothic
- Yu Gothic UI
- Yu Gothic
- Meiryo
- MS Gothic
- MS Mincho
- sans-serif
- serif

> フォントファイル自体は同梱しない。ライセンス不明のフォントを勝手にバンドルしない。

#### D. 吹き出しと文字の関連付け

「吹き出し画像」と「文字」はFabric.js上では別オブジェクトとして保持する。ただしアプリのデータモデル上は `pairId` で関連付け可能にする。

目的:

- 吹き出しを拡大しても、文字サイズまで比例して巨大化しない。
- 文字だけ位置調整できる。
- 必要時は吹き出し移動に文字を追従させられる。

仕様:

- 新しい吹き出しを追加した直後、「文字を追加」ボタンで対応テキストを作れる。
- `moveLinked = true` の場合、吹き出し移動量に合わせて文字も移動する。
- 吹き出しの拡縮は既定では文字サイズへ影響させない。
- 「文字を吹き出し中央へ」ボタンを用意する。
- 関連付け解除も可能にする。

#### E. 図形とスポイト

- 四角形追加。
- 角丸四角形追加。
- 円 / 楕円追加。
- 塗り色変更。
- 線色変更。
- 線幅変更。
- 不透明度変更。
- スポイトで画像から色を取得。
- 取得色を文字色、文字縁取り色、図形塗り色、図形線色に適用可能。

#### F. レイヤー

- 右パネル内にレイヤー一覧。
- アイコンと名前を表示。
- ベース画像は最下層で固定。
- テンプレ名、テキスト先頭文字列を自動でレイヤー名にする。
- ドラッグで順序変更。
- 表示/非表示。
- ロック/解除。
- 複数選択。
- 削除。

#### G. Undo / Redo

- 最低50履歴。
- 次の操作を履歴対象とする。
  - 追加
  - 削除
  - 移動
  - 拡縮
  - 回転
  - 色変更
  - フォント変更
  - 文字サイズ変更
  - 文字編集
  - レイヤー順序変更
  - 反転
  - 不透明度変更
- ドラッグ中のmousemoveごとに履歴を積まない。操作完了イベント単位で記録する。

#### H. 書き出し

- PNGを必須。
- JPEG / WebPを追加対応してよい。
- PNGは既定で元画像と同じピクセル寸法。
- 画像サイズごとに書き出し寸法が正しく一致すること。
- JPEG / WebPは品質スライダーを表示。
- ファイル名の既定値は元画像名を基にする。
  - 例: `source.png` → `source_composed.png`
- 書出し時、選択枠・ハンドル・ガイド線を含めない。
- 透明背景プロジェクトを将来扱えるよう、エクスポート関数は背景色を固定しない。

#### I. 保存と復旧

MVPでは「単一ファイルの高度なプロジェクト形式」を最優先にしない。まず作業喪失防止を重視する。

- 現在の編集状態をアプリデータ領域へ自動保存。
- 変更後1秒程度のデバウンスで保存。
- アプリ異常終了後、次回起動時に復元候補を表示。
- 最近使ったプロジェクトを最大10件程度表示。
- 最近使った吹き出しテンプレを保存する。
- 最近使った色を保存する。
- 最近使った文字スタイルを保存する。
- 最近使った図形スタイルを保存する。
- プロジェクトデータはバージョン番号を持つ。
- 将来の `*.fksproj` ポータブル形式へ移行できるよう、I/O層を分離する。

推奨内部構成:

```text
appData/
└─ projects/
   └─ <project-id>/
      ├─ project.json
      ├─ assets/
      │  ├─ base-image.*
      │  └─ imported-*.png
      └─ preview.webp
```

### 1-3. MVPでやらないもの

- Photoshop級の画像補正。
- ブラシ描画。
- 消しゴム。
- 自由選択。
- OCR。
- AIによる吹き出し検出。
- AIによる自動文字配置。
- クラウド保存。
- アカウント機能。
- リアルタイム共同編集。
- 外部公開サーバー。
- 課金機能。
- テンプレマーケット。
- SVGの無制限読み込み。

### 1-4. 将来機能候補

優先順位の高い順。

1. 縦書き日本語テキスト。
2. カスタムフォントファイルの登録。
3. 文字の縁取り、影、背景。
4. 吹き出しと文字のスタイルプリセット保存。
5. よく使うテンプレのお気に入り。
6. テンプレカテゴリと検索。
7. 複数画像の連続処理。
8. 「前の画像と同じ吹き出し配置」を別画像へ適用。
9. ポータブルプロジェクト形式 `*.fksproj`。
10. Android / iOS UI。
11. タッチジェスチャー最適化。

> 縦書きは日本語漫画用途に重要だが、禁則処理、約物、長音符、句読点回転等が絡む。MVPの横書きを確実に完成させた後、専用仕様として追加する。

---

## 2. 採用する構成

### 2-1. 推奨技術

| 技術 | 役割 |
|---|---|
| Tauri 2 | デスクトップアプリ化、ファイルダイアログ、保存、将来のモバイル展開 |
| React | UIコンポーネント |
| TypeScript | 型安全と保守性 |
| Vite | フロントエンド開発・ビルド |
| Fabric.js | Canvas上の画像、テキスト、選択、変形、シリアライズ |
| Zustand | UI状態・プロジェクト状態・履歴管理の補助。Fabricのmutable objectを直接全状態の真実にしない |
| Python 3 | 画像検査、テンプレ前処理、差分検証、バッチ処理、テスト支援、開発自動化、Codexのトークン節約を目的とした決定論的処理 |
| Pillow | Python側の画像メタデータ取得、サムネイル生成、画像差分、形式検証等 |
| PyInstaller | PC版で必要なPythonツールをsidecar実行ファイル化し、利用者側のPython導入を不要にする候補 |
| Tailwind CSS | 一貫した現代的UIの構築 |
| Radix UI primitives | Dialog、Popover、Slider、Dropdown等のアクセシブルな土台。必要箇所だけ使う |
| Vitest | TypeScriptロジックの単体テスト |
| React Testing Library | UIの主要動作テスト |
| Playwright | 必要ならWeb部分のE2E。Tauri本体E2Eが難しい場合は主要フローをWebモードで検証 |

### 2-2. Pythonの位置付け

PythonはGUI本体を置き換えるためではなく、以下の3用途で積極的に使用する。

1. **アプリ機能の補助エンジン**
   - 画像形式検証
   - 画像サイズ・EXIF・透過情報の取得
   - サムネイル生成
   - 大量テンプレの一括登録前処理
   - 画像差分検証
   - 将来のバッチ合成や自動レイアウト候補計算

2. **開発・テスト自動化**
   - テスト画像生成
   - fixture生成
   - エクスポート結果のピクセル差分
   - プロジェクトJSONのlint
   - schema migration検証
   - 大量ログの分類・要約
   - ベンチマーク測定とJSON/Markdownレポート生成

3. **Codexのトークン消費削減**
   - 大量のファイル内容をLLMへ貼り付けない。
   - まずPythonで走査・集計・異常抽出する。
   - Codexには「要約JSON」「失敗箇所」「統計値」「差分上位項目」だけを渡す。
   - 同じ形式の反復作業は、その都度Codexに考えさせずPythonスクリプト化して再利用する。
   - 画像100枚の寸法・透過有無確認等を、画像100枚分の個別LLM判断にしない。
   - 長大ログはPythonでエラー種別、頻度、最初の発生箇所、代表例だけに縮約する。

> 原則として、**決定論的に書ける作業はPythonへ寄せ、判断が必要な部分だけCodexへ残す。**
> これを本プロジェクトの `Script-first policy` とする。

### 2-3. 構成図

```text
┌─────────────────────────────────────────────────────┐
│                    Tauri 2 App                      │
│                                                     │
│  ┌───────────────────────────────────────────────┐  │
│  │ React / TypeScript UI                         │  │
│  │                                               │  │
│  │  Toolbar   Asset Panel   Inspector   Layers   │  │
│  │                  │                            │  │
│  │                  ▼                            │  │
│  │            Editor Commands                    │  │
│  │                  │                            │  │
│  │        ┌─────────┴─────────┐                  │  │
│  │        ▼                   ▼                  │  │
│  │  Fabric Adapter      Project Store            │  │
│  │        │                   │                  │  │
│  │        ▼                   ▼                  │  │
│  │   Fabric.js Canvas     History / Metadata     │  │
│  └───────────────┬───────────────────────────────┘  │
│                  │ Platform Adapter                  │
│                  ▼                                   │
│  ┌───────────────────────────────────────────────┐  │
│  │ Tauri I/O Adapter                             │  │
│  │ Dialog / File Save / App Data / Path          │  │
│  └──────────────────┬────────────────────────────┘  │
│                     │ optional sidecar               │
│                     ▼                                │
│  ┌───────────────────────────────────────────────┐  │
│  │ Python Tools / Image Worker                   │  │
│  │ Pillow / Validation / Diff / Batch / Reports  │  │
│  └───────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

### 2-4. この構成を選ぶ理由

- UIとCanvas編集処理をWeb技術側に置くことで、Tauri固有機能を薄いI/Oアダプターに限定できる。
- 将来モバイル対応するとき、Canvas編集ロジックと大半のReact UIを再利用しやすい。
- Fabric.jsのCanvasオブジェクトモデルを利用し、ドラッグ、拡縮、回転、テキスト編集をゼロから再実装しない。
- TauriはMVPで必要なローカルファイル操作だけを許可し、外部サーバーを不要にする。
- Pythonは常駐APIサーバーとして必須化せず、必要な処理だけCLI/sidecarとして呼び出す。GUIがPythonプロセスの生存に全面依存する構造を避ける。
- Pythonで大量データを事前処理し、Codexへ返す情報量を減らすことで、開発時のLLMコンテキスト消費を抑える。

### 2-5. アーキテクチャ上の禁止事項

- Reactコンポーネントから直接 Tauri API を乱用しない。
- Reactコンポーネントから Fabric Canvas インスタンスを無秩序に直接操作しない。
- 1つの巨大な `App.tsx` に全ロジックを集約しない。
- Canvasの表示倍率と実データ座標を混同しない。
- Undo/Redo履歴をマウス移動イベントごとに保存しない。
- Pythonで済む決定論的な大量確認作業を、1件ずつCodexへ読ませない。
- Python sidecarを理由なく常駐HTTPサーバー化しない。まずCLIのstdin/stdout JSON通信を優先する。
- Python実行結果として巨大なstdoutを返さない。機械可読な要約JSONと必要最小限の詳細パスを返す。

推奨レイヤー:

```text
UI Component
    ↓ command
Editor Command / UseCase
    ↓
Fabric Adapter + Project Store
    ↓
Platform Adapter
    ↓
Tauri Plugin
```

---

## 3. 環境・配置

### 3-1. 環境変数

| 変数 | 意味 | Windows例 |
|---|---|---|
| `<WORKDIR>` | プロジェクトのソースコード | `D:\dev\fukidashi-studio` |
| `<OS>` | 対象OS | Windows 11 |
| `<SHELL>` | シェル | PowerShell |
| `<PKG>` | JSパッケージ管理 | `pnpm` 推奨。ただし既存環境に合わせて npm でもよい |
| `<RUST>` | Rustツールチェーン | rustup / cargo |

### 3-2. 配置方針

`<WORKDIR>`:

- ソースコード
- 設計書
- README
- STATUS.md
- テスト
- ドキュメント
- 最終レポート

OSのアプリデータ領域:

- ユーザー登録テンプレ
- サムネイルキャッシュ
- 自動保存プロジェクト
- 最近使ったプロジェクト情報
- ユーザー設定

元画像:

- ユーザーが選択した元ファイルを直接破壊しない。
- 編集用コピーをアプリデータ領域へ保存する。
- エクスポートは別ファイルとして保存する。

### 3-3. マルチ環境対応

- Canvas編集ロジックは純粋TypeScriptに寄せる。
- ファイルI/Oは `PlatformFileService` インターフェースの背後に隠す。
- PC版は `TauriFileService` を実装する。
- 将来Web版を作る場合は `BrowserFileService` を追加できるようにする。
- モバイルでは右インスペクターをドロワーまたはボトムシートに切り替えられる構造にする。
- Pointer Eventsを優先し、マウス専用イベントへ過剰依存しない。

### 3-4. シークレット

MVPではAPIキー等は不要。

| 名前 | 用途 | 置き場所 |
|---|---|---|
| なし | 外部APIを使用しない | なし |

---

## 4. 事前チェック

Codexは実装開始前に以下を確認する。

### 4-1. 公式一次ソースでバージョン確認

最低限、以下の公式情報を確認する。

- Tauri v2 Docs: https://v2.tauri.app/
- Tauri Dialog Plugin: https://v2.tauri.app/plugin/dialog/
- Fabric.js Docs: https://fabricjs.com/
- Vite Guide: https://vite.dev/guide/
- React Docs: https://react.dev/

依存バージョンは設計書に固定値として盲目的にコピーせず、着手時点の安定版を選定する。

### 4-2. Windows前提チェック

PowerShell等で確認する。

```powershell
node --version
pnpm --version
rustc --version
cargo --version
python --version
py -0p
```

Python側の仮想環境も作成する。

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
```

TauriのWindows前提:

- Microsoft C++ Build Tools
- Microsoft Edge WebView2
- Rust toolchain
- Node.js

不足時は公式Tauri prerequisite手順に従う。

### 4-3. 最小スモーク

本実装前に以下を通す。

1. Tauri + React + TypeScriptの最小アプリが起動する。
2. Fabric.jsで1000×1000程度のCanvasを表示する。
3. 透明PNGを1枚追加してドラッグ・拡縮できる。
4. 日本語テキストを追加してIME入力できる。
5. PNGのData URLまたはBlobを生成できる。
6. Tauriの保存ダイアログを開ける。
7. 選択した保存先へ最小PNGを書き込める。
8. Python CLIへ画像パスを渡し、幅・高さ・形式・alpha有無をJSONで返せる。
9. TauriからPython sidecarを1回呼び出し、stdout JSONを受信できる。

このスモークが通るまで本格UIを作り込まない。

---

## 5. タスク分割とチェックポイント

| CP | 内容 | 成果物 | 受け入れ条件 |
|---|---|---|---|
| CP1 | 事前チェック、Tauri骨格、Python環境、最小スモーク | 起動する最小アプリ、Python CLI | 画像、テキスト、PNG保存、Python JSON応答の最小経路が通る |
| CP2 | Canvasコア、ベース画像読込、ズーム/パン | EditorCanvas関連 | 画像表示倍率と書出し解像度が分離している |
| CP3 | 吹き出しテンプレ登録・追加・変形 | AssetPanel、TemplateService | 任意透明PNGを登録し、追加、移動、拡縮、回転、反転できる |
| CP4 | テキスト編集とインスペクター | TextInspector等 | 字体、サイズ、色、行間、字間、揃えが変更できる |
| CP5 | ペア関連付け、レイヤー、Undo/Redo | LayerPanel、HistoryManager | 主要操作を50履歴以内でUndo/Redoできる |
| CP6 | 自動保存、復旧、最近使った項目 | ProjectRepository | 異常終了想定後も直前状態を復元できる |
| CP7 | 書き出し、ファイル名、品質設定 | ExportService | 元画像ピクセル寸法でPNGを書き出せる |
| CP8 | UI/UX仕上げ、ショートカット、レスポンシブ基盤 | 完成UI | 古い業務ソフト風でなく、一貫した現代的UI |
| CP9 | 自動テスト、Python検証ツール、手動テスト、Windowsビルド | テスト結果、検証レポート、インストーラ | 7章の合格ラインを満たす |
| CP10 | 手順書、最終レポート | docs一式 | Chamiが手順書だけで起動・操作できる |

---

## 6. 実装方針

### 6-1. Chamiが手でやること

最小限にする。

1. `<WORKDIR>` を指定する。
2. Windowsの管理者権限が必要な前提ツール導入を許可・実行する。
3. 実際の吹き出しテンプレPNGを用意する。
4. 最終検収時に実画像で操作する。

これ以外はCodexが代行する。

### 6-2. 推奨ディレクトリ構成

```text
fukidashi-studio/
├─ src/
│  ├─ app/
│  │  ├─ App.tsx
│  │  ├─ routes/
│  │  └─ providers/
│  ├─ components/
│  │  ├─ toolbar/
│  │  ├─ asset-panel/
│  │  ├─ inspector/
│  │  ├─ layer-panel/
│  │  ├─ canvas/
│  │  └─ common/
│  ├─ editor/
│  │  ├─ commands/
│  │  ├─ fabric/
│  │  ├─ history/
│  │  ├─ selection/
│  │  └─ export/
│  ├─ project/
│  │  ├─ model/
│  │  ├─ repository/
│  │  ├─ migration/
│  │  └─ autosave/
│  ├─ platform/
│  │  ├─ types.ts
│  │  └─ tauri/
│  ├─ store/
│  ├─ hooks/
│  ├─ styles/
│  └─ tests/
├─ src-tauri/
│  ├─ src/
│  ├─ capabilities/
│  └─ tauri.conf.json
├─ python/
│  ├─ src/
│  │  ├─ cli.py
│  │  ├─ image_inspect.py
│  │  ├─ thumbnail.py
│  │  ├─ image_diff.py
│  │  ├─ project_lint.py
│  │  ├─ log_summarize.py
│  │  └─ report.py
│  ├─ tests/
│  ├─ fixtures/
│  ├─ requirements.txt
│  └─ README.md
├─ scripts/
│  ├─ dev_check.py
│  ├─ test_all.py
│  └─ build_report.py
├─ docs/
│  ├─ USER_GUIDE.md
│  └─ FINAL_REPORT.md
├─ STATUS.md
├─ README.md
└─ package.json
```

### 6-3. データモデル

例。実装時に型を整理すること。

```ts
type ProjectDocument = {
  schemaVersion: number;
  projectId: string;
  name: string;
  createdAt: string;
  updatedAt: string;
  canvas: {
    width: number;
    height: number;
    backgroundAssetId: string;
  };
  objects: EditorObject[];
  settings: {
    exportFormat: 'png' | 'jpeg' | 'webp';
    exportQuality: number;
  };
};

type EditorObject =
  | BubbleObject
  | TextObject;

type BubbleObject = {
  id: string;
  type: 'bubble';
  assetId: string;
  pairId?: string;
  transform: Transform;
  opacity: number;
  visible: boolean;
  locked: boolean;
  zIndex: number;
};

type TextObject = {
  id: string;
  type: 'text';
  pairId?: string;
  text: string;
  fontFamily: string;
  fontSize: number;
  fill: string;
  fontWeight: number | string;
  fontStyle: 'normal' | 'italic';
  textAlign: 'left' | 'center' | 'right';
  lineHeight: number;
  charSpacing: number;
  transform: Transform;
  visible: boolean;
  locked: boolean;
  zIndex: number;
};

type Transform = {
  x: number;
  y: number;
  scaleX: number;
  scaleY: number;
  rotation: number;
  flipX: boolean;
  flipY: boolean;
};
```

### 6-4. Fabric.jsとの同期

Fabric objectをReact stateへそのまま保存しない。

推奨:

- `FabricEditorAdapter` がCanvasインスタンスを所有。
- UIはcommandを発行。
- command実行後、必要なシリアライズ可能状態をProject Storeへ同期。
- `object:modified` 等の完了イベントで履歴スナップショットを作る。
- ドラッグ中イベントでは履歴保存しない。
- Canvas再生成時はProjectDocumentから復元する。

必要なインターフェース例:

```ts
interface EditorEngine {
  loadBaseImage(asset: AssetRef): Promise<void>;
  addBubble(asset: AssetRef): Promise<string>;
  addText(initialText?: string): string;
  updateObject(id: string, patch: EditorObjectPatch): void;
  removeObjects(ids: string[]): void;
  duplicateObjects(ids: string[]): void;
  moveLayer(id: string, direction: LayerMove): void;
  setZoom(zoom: number): void;
  fitToViewport(): void;
  exportImage(options: ExportOptions): Promise<Blob>;
  serialize(): ProjectDocument;
  restore(project: ProjectDocument): Promise<void>;
}
```

### 6-5. Undo / Redo設計

単純スナップショット方式をMVP推奨とする。

- `past` 最大50。
- `present` 1。
- `future` 最大50。
- 操作完了時にProjectDocumentの軽量スナップショットを積む。
- 画像バイナリを毎回履歴へ複製しない。
- 履歴はオブジェクト状態と参照IDだけを保持。
- 連続スライダー操作はpointer up / change commitで1履歴にまとめる。
- 文字入力は500〜800ms程度のデバウンスか、編集終了時に1履歴とする。

### 6-6. テンプレライブラリ

最低限のメタデータ:

```ts
type TemplateAsset = {
  id: string;
  name: string;
  originalFileName: string;
  mimeType: string;
  width: number;
  height: number;
  storedPath: string;
  thumbnailPath: string;
  createdAt: string;
};
```

仕様:

- インポート時にアプリデータ領域へコピー。
- サムネイルを生成して一覧表示を高速化。
- 元ファイルを削除しても登録済みテンプレが使える。
- 同一ファイル重複登録はハッシュで検知できれば望ましい。
- 削除時は確認ダイアログを出す。
- 使用中のプロジェクトが参照しているテンプレを削除する場合は警告する。

### 6-7. エクスポート設計

**重要:** 画面上の表示サイズをそのままキャプチャしてはいけない。

- エディタの論理Canvasサイズを元画像ピクセル寸法にする。
- CSS上でCanvasの見た目だけ縮小して表示するか、viewportTransformを使う。
- 書出し時は選択解除。
- 必要なら一時的にガイド・コントロールを非表示。
- PNG Blobを生成。
- Tauri保存ダイアログで保存先選択。
- バイナリ書込み。
- 保存完了通知を表示。

### 6-8. Python統合方針

#### 通信方式

MVPではPython処理を小さなCLIとして設計する。

推奨:

```text
Tauri / Rust
   ↓ spawn sidecar
Python CLI
   ↓ read input JSON
Processing
   ↓
stdout: compact JSON
stderr: diagnostic log
exit code: success/failure
```

例:

```json
{
  "command": "inspect_image",
  "input": {
    "path": "<selected-file>"
  }
}
```

返却例:

```json
{
  "ok": true,
  "data": {
    "format": "PNG",
    "width": 1122,
    "height": 1402,
    "hasAlpha": true
  },
  "warnings": []
}
```

ルール:

- stdoutは機械可読JSON専用。
- デバッグログはstderr。
- 1コマンド1責務。
- タイムアウトを設定。
- 終了コードを定義。
- パス・入力値を検証。
- 失敗時は巨大なtraceback全文をUIへ出さず、ログファイルへ保存してUIには短い説明を返す。
- 常駐プロセスが明確に有利になるまで、単発CLIを優先する。

#### Pythonへ任せる候補

**アプリ実行時:**

- `inspect_image`
  - 形式
  - 幅、高さ
  - alpha有無
  - EXIF orientation
  - 読み込み可否

- `create_thumbnail`
  - テンプレライブラリ用WebPサムネイル生成

- `validate_template`
  - 透明領域の有無
  - 異常サイズ
  - 破損
  - 形式確認

**開発時:**

- `image_diff`
  - expectedとactualの差分率
  - 差分bbox
  - diff画像出力

- `project_lint`
  - schemaVersion
  - 存在しないassetId参照
  - 重複ID
  - 不正zIndex
  - pairId孤立

- `log_summarize`
  - エラー種類別件数
  - 最初の発生位置
  - 代表スタック
  - 頻出メッセージ
  - 時系列集約

- `build_report`
  - テスト結果
  - ビルド結果
  - ファイルサイズ
  - ベンチマーク
  - warning一覧
  - Markdownレポート生成

### 6-9. Codexトークン節約ルール

このプロジェクトでは、LLMトークンを計算資源として管理する。

#### Rule 1: 大量入力の前にPythonで縮約

悪い例:

```text
100個の画像情報をCodexへ個別に貼り、問題画像を探させる
```

良い例:

```text
Pythonで100個を走査
→ invalid 3件
→ unusually_large 2件
→ alpha_missing 6件
→ その11件だけCodexが確認
```

#### Rule 2: ログ全文を貼らない

Pythonで以下に縮約する。

```json
{
  "totalLines": 18422,
  "errorGroups": [
    {
      "type": "PermissionDenied",
      "count": 83,
      "firstOccurrence": "logs/app.log:913",
      "sample": "..."
    }
  ]
}
```

Codexは必要なグループだけ追加調査する。

#### Rule 3: 反復作業を再プロンプト化しない

2回以上繰り返す可能性がある定型処理は、原則として次のどれかへ変換する。

- Python script
- npm script
- test
- lint rule
- codemod
- fixture generator

#### Rule 4: Codexへ返す結果に上限を設ける

Pythonツールの標準返却は次を目安にする。

- summary: 数十行以内
- top failures: 最大20件
- representative samples: 種類ごとに最大3件
- raw detail: ファイルへ保存し、パスだけ返す

#### Rule 5: 画像比較を目視LLMだけに頼らない

エクスポート回帰テストはまずPython/Pillowで比較する。

1. 寸法一致確認。
2. mode一致確認。
3. pixel difference計算。
4. 差分bbox。
5. 差分率。
6. 必要な場合だけ人間またはCodexが画像確認。

#### Rule 6: リポジトリ調査も機械抽出を先に行う

Codexが全ファイルを逐次読む前にPythonで以下を出せるようにする。

- 拡張子別ファイル数
- 大きなファイル上位
- TODO / FIXME一覧
- import依存関係の概要
- 重複候補
- テスト未対応候補
- 最近変更されたファイル一覧

#### Rule 7: Python使用判断を毎回ユーザーへ確認しない

安全なローカル処理であり、既存ファイルを破壊せず、課金・外部送信を伴わない場合、CodexはPythonを自由に使用してよい。

ただし以下は確認する。

- 既存データの削除
- 元画像上書き
- 大規模一括リネーム
- 外部通信
- 外部API
- 課金
- 個人データの外部送信

### 6-10. Python sidecarの配布方針

PC版では利用者にPythonを別途インストールさせない構成を優先する。

候補:

```text
python source
   ↓
PyInstaller
   ↓
standalone executable
   ↓
Tauri sidecar
   ↓
Windows app bundle
```

注意:

- 開発中は `.venv` のPythonで高速に反復してよい。
- リリース時はPythonバージョンと依存を固定する。
- PyInstallerビルドは対象OS上で行う。
- sidecarサイズ増加を実測する。
- Pythonでしか合理的にできない処理だけをsidecarへ入れる。
- trivialな処理までPythonプロセス起動に回さない。
- スマホ版では同じPython sidecar方式を前提にしない。将来モバイル対応時に、純TypeScript/Rust移植または別方式を判断する。

### 6-11. キーボードショートカット

Windows基準:

| 操作 | ショートカット |
|---|---|
| Undo | Ctrl+Z |
| Redo | Ctrl+Y / Ctrl+Shift+Z |
| 複製 | Ctrl+D |
| 削除 | Delete / Backspace。ただし文字編集中は文字削除を優先 |
| 保存 | Ctrl+S |
| エクスポート | Ctrl+Shift+E |
| 全選択 | Ctrl+A。文字編集中はテキスト編集側を優先 |
| 選択解除 | Esc |
| ズームイン | Ctrl++ |
| ズームアウト | Ctrl+- |
| 100% | Ctrl+0 |
| パン | Space + Drag |
| 微移動 | Arrow |
| 10px移動 | Shift + Arrow |

ショートカットは入力欄、IME変換中、キャンバステキスト編集中の衝突をテストする。

---

## 7. UI / UX仕様

### 7-1. 全体レイアウト

デスクトップ（標準レイアウト: 左操作 / 右プレビュー）:

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ Header: Open Save Export | Undo Redo | Zoom | Layout Swap                   │
├───────────────────────────────┬──────────────────────────────────────────────┤
│ Left Control Tabs             │ Right Preview / Canvas                      │
│ ┌───────────────────────────┐ │                                              │
│ │ テキスト                 │ │              縦長漫画画像プレビュー           │
│ │ 吹き出し                 │ │                                              │
│ │ 図形                     │ │                                              │
│ │ 素材                     │ │                                              │
│ │ 履歴                     │ │                                              │
│ │ 設定                     │ │                                              │
│ └───────────────────────────┘ │                                              │
├───────────────────────────────┴──────────────────────────────────────────────┤
│ Bottom Property Panel: text / bubble / shape settings (live preview)        │
├──────────────────────────────────────────────────────────────────────────────┤
│ Status: image size | zoom | selection | writing mode | current color        │
└──────────────────────────────────────────────────────────────────────────────┘
```

設定で左右反転した場合は、Preview / Canvas と Left Control Tabs の位置を入れ替える。


推奨幅の目安:

- 左パネル: 220〜260px。
- 右パネル: 300〜340px。
- 中央は残り全て。
- 画面幅が狭い場合、パネルを折りたためる。

### 7-2. 視覚デザイン

古いデスクトップGUIにしない。

**方向性:**

- ダークテーマを既定。
- ニュートラルなチャコール / スレート基調。
- アクセントは1色。
- 大きなグラデーションや過剰なネオンは使わない。
- 角丸は10〜14px程度を中心。
- ボタン高さ36〜40px程度。
- タッチ展開を考える操作対象は44px前後を意識。
- アイコン + ツールチップ。
- 常時表示テキストは必要最小限。
- セクション間は余白と薄い境界線で分ける。
- 影は強くしない。
- フォーム要素の高さ・余白を統一する。
- WindowsのクラシックUI風の灰色ボタン列を避ける。

フォントスタック:

```css
font-family:
  "BIZ UDPGothic",
  "Noto Sans JP",
  "Yu Gothic UI",
  "Meiryo",
  sans-serif;
```

### 7-3. コンポーネント挙動

#### Header

- 左: アプリ名は小さく。
- Open。
- Save。
- ExportをPrimary Actionとして最も目立たせる。
- Undo/Redo。
- Zoom表示。

#### Asset Panel

タブ:

- テキスト。
- 吹き出し。
- 図形。
- 素材。
- 履歴。
- 設定。

テンプレ:

- 2列または3列サムネイル。
- hoverで名前。
- クリックで追加。
- コンテキストメニューで名前変更・削除。
- 上部にImportボタン。

#### Canvas Area

- 背景はCanvasと区別できる暗めの作業領域。
- Canvasそのものは画像に従う。
- 余白を十分取る。
- 選択ハンドルは現代的で細すぎない。
- 回転ハンドルを明確にする。
- 選択中オブジェクトの種類をStatus Barへ表示。

#### Inspector

選択なし:

- Canvas情報。
- 画像サイズ。
- 書出し設定への導線。

Bubble選択:

- Position X/Y。
- Width/Height。
- Aspect Lock。
- Rotation。
- Opacity。
- Flip X/Y。
- Link Text。
- Center Text。

Text選択:

- Textarea。
- Font Family。
- Font Size。
- Color Picker + Hex入力。
- Bold。
- Italic。
- Align。
- Line Height。
- Letter Spacing。
- Width。
- Link Bubble。

Layers:

- 右パネル下部または独立タブ。
- 行の高さを詰めすぎない。
- visible / lockをアイコンで操作。

### 7-4. 空状態

起動直後:

```text
画像をここへドロップ
または
[画像を開く]

PNG / JPEG / WebP
```

テンプレが0件:

```text
吹き出しテンプレがありません
[テンプレを追加]
```

### 7-5. エラー表示

- OSダイアログ連発ではなく、通常エラーはアプリ内Toast。
- 保存失敗等の重大エラーはDialog。
- 技術用語だけのエラーメッセージを出さない。
- 詳細ログは開発者用ログへ分離。

例:

```text
この画像を読み込めませんでした。
対応形式: PNG / JPEG / WebP
```

---

## 8. テストと合格ライン

### 8-1. テストタイミング

1. 各CP終了時。
2. CanvasとUI統合時。
3. 保存/復元統合時。
4. Windowsビルド前。
5. 最終納品前。

### 8-2. 自動テスト対象

最低限:

- ProjectDocument serialization。
- ProjectDocument migration。
- HistoryManagerのpush / undo / redo / limit。
- pairId関連付け。
- bubble移動時のlinked text追従量。
- layer reorder。
- export file name生成。
- zoom計算。
- fit-to-viewport計算。
- color / font size validation。
- autosave debounce。
- Python `inspect_image` の正常系・破損系。
- Python `project_lint` の孤立pairId検出。
- Python `image_diff` の同一画像・差分画像判定。
- Python JSON CLIのstdout/stderr分離。

### 8-3. 手動テストシナリオ

#### Scenario 1: 基本フロー

1. 852×1280 PNGを開く。
2. 透明PNG吹き出しを登録。
3. キャンバスへ追加。
4. 移動。
5. 拡大。
6. 回転。
7. 文字追加。
8. 日本語入力。
9. フォント変更。
10. 文字サイズ変更。
11. 文字色変更。
12. PNG書出し。
13. 出力画像が852×1280であることを確認。
14. 1022×1536 PNGでも同様の流れを再実施。
15. 出力画像が1022×1536であることを確認。

合格条件:

- エラーなし。
- 透明部分が正しく合成される。
- 日本語が文字化けしない。
- 書出し画像に選択枠が入らない。
- サイズごとに書出し寸法が一致する。

#### Scenario 1B: 別サイズ追加確認

1. 1122×1402 PNGを開く。
2. 透明PNG吹き出しを登録。
3. キャンバスへ追加。
4. 移動。
5. 拡大。
6. 回転。
7. 文字追加。
8. 日本語入力。
9. フォント変更。
10. 文字サイズ変更。
11. 文字色変更。
12. PNG書出し。
13. 出力画像が1122×1402であることを確認。

合格条件:

- エラーなし。
- 透明部分が正しく合成される。
- 日本語が文字化けしない。
- 書出し画像に選択枠が入らない。

#### Scenario 2: Undo / Redo

1. 吹き出し追加。
2. 移動。
3. 拡縮。
4. 文字追加。
5. 色変更。
6. 5回Undo。
7. 5回Redo。

合格条件:

- 状態が順序通り戻り、再度進む。
- 画像レイヤーが消失しない。

#### Scenario 3: 復旧

1. 作業を変更。
2. 自動保存完了を待つ。
3. アプリを閉じる。
4. 再起動。
5. 復旧する。

合格条件:

- オブジェクト位置、文字、色、レイヤー順が復元される。

#### Scenario 4: 異常系

- 0byteファイル。
- 拡張子偽装。
- 非対応ファイル。
- 壊れたPNG。
- 極端に大きい画像。
- 保存先権限なし。
- テンプレ元ファイル削除後の利用。

合格条件:

- アプリがクラッシュしない。
- 意味のあるエラーが出る。

### 8-4. 性能の目安

対象PCの目安:

- Intel Core i7-11700
- RAM 32GB
- RTX 3060 Ti

GPU性能に依存しすぎないこと。

| 指標 | 合格目安 |
|---|---|
| アプリ起動 | 数秒以内を目標。実測値を報告 |
| 1122×1402画像読込 | 体感待ちが少ない。実測値を報告 |
| 10〜20オブジェクト操作 | ドラッグが明確にカクつかない |
| Undo / Redo | 体感即時 |
| PNG書出し | 数秒以内を目標。実測値を報告 |
| メモリ | 異常増加がない。連続編集でリーク傾向を確認 |

数値は実機で測定し、最終レポートへ書く。推測値で「高速」と報告しない。

### 8-5. 日本語入力テスト

必須文字列例:

```text
久しぶり、ゆう君…
えっ！？　本当に？
めっちゃ狙われてる気がする…
「私でいいの？」
```

確認項目:

- IME変換中にショートカットが誤発火しない。
- `…`、`！？`、かぎ括弧、全角空白が保持される。
- 保存・復元後に文字化けしない。
- エクスポート画像で字形が変わらない。

---

## 9. 運用の保険

### 9-1. やり直し

- ソースはgitで戻す。
- アプリデータ削除はソース削除と分ける。
- アンインストール時にユーザーテンプレとプロジェクトを自動削除しない方針を優先する。
- データ削除は設定画面から明示的に実行する。

### 9-2. バックアップ

バックアップ対象:

- `<WORKDIR>` のソース。
- アプリデータ内の `projects/`。
- アプリデータ内の `templates/`。

復旧:

1. アプリを終了。
2. 同一schemaVersion対応版を用意。
3. appData配下へデータを戻す。
4. 起動してmigrationと整合性検査を行う。

### 9-3. 公開範囲

- MVPはローカル限定。
- Webサーバー公開なし。
- LAN公開なし。
- 外部通信を前提にしない。

### 9-4. 更新運用

- 依存更新は月1回程度の保守タイミングでまとめて検討。
- Tauri / Fabric.jsのメジャー更新は移行ガイドを読んでから行う。
- 依存更新前にgit commitとバックアップ。
- 更新後は7章の基本フローと保存復旧を再テストする。

---

## 10. 納品物

### 10-1. アプリ本体

- Windowsで起動できるビルド成果物。
- 可能ならインストーラ。
- 開発起動コマンド。
- 本番ビルドコマンド。

### 10-2. ソースコード

- `<WORKDIR>` 一式。
- git履歴。
- `.gitignore`。
- 依存ロックファイル。

### 10-3. 利用手順書

`docs/USER_GUIDE.md` またはオフラインHTML。

必須内容:

1. 起動方法。
2. 画像を開く。
3. テンプレ登録。
4. 吹き出し配置。
5. テキスト編集。
6. レイヤー操作。
7. Undo/Redo。
8. 保存と復旧。
9. 書き出し。
10. よくあるエラー。
11. バックアップ場所。

HTMLにする場合:

- UTF-8。
- 日本語フォントスタック指定。
- レスポンシブ。
- 外部CDNなし。
- 実際に開いて文字化け確認。

### 10-4. 最終レポート

`docs/FINAL_REPORT.md`

必須項目:

1. 実際に起きた問題。
2. 想定外だったこと。
3. 設計書とのズレ。
4. 採用バージョンと選定理由。
5. テスト結果。
6. 未解決事項。
7. セキュリティ判断。
8. 性能実測値。
9. モバイル化時に残る課題。
10. 次回改善提案。

---

## 11. 完了チェックリスト

### 進め方

- [ ] 実装前に設計書を通読した。
- [ ] CP計画を提示した。
- [ ] STATUS.mdを更新した。
- [ ] CP単位でgit commitした。

### 基本機能

- [ ] 画像を開ける。
- [ ] ドラッグ&ドロップで画像を開ける。
- [ ] 透明吹き出しテンプレを登録できる。
- [ ] 吹き出しを追加できる。
- [ ] 移動できる。
- [ ] 拡縮できる。
- [ ] 回転できる。
- [ ] 左右/上下反転できる。
- [ ] 不透明度を変えられる。
- [ ] 日本語テキストを追加できる。
- [ ] フォントを変更できる。
- [ ] フォントサイズを変更できる。
- [ ] 文字色を変更できる。
- [ ] 行間を変えられる。
- [ ] 字間を変えられる。
- [ ] 揃えを変えられる。
- [ ] レイヤー順を変えられる。
- [ ] ロックできる。
- [ ] 表示/非表示を切り替えられる。
- [ ] 複製できる。
- [ ] 削除できる。
- [ ] Undo/Redoできる。
- [ ] 自動保存できる。
- [ ] 復旧できる。
- [ ] PNG書出しできる。
- [ ] 出力サイズが元画像サイズと一致する。
- [ ] 852×1280px で正常動作する。
- [ ] 1022×1536px で正常動作する。
- [ ] 1122×1402px で正常動作する。

### UI/UX

- [ ] 空状態が分かりやすい。
- [ ] 主要操作がGUIから可能。
- [ ] Exportが見つけやすい。
- [ ] Inspectorが選択対象に応じて変わる。
- [ ] Toastと重大Dialogを使い分けている。
- [ ] 古い業務ソフト風の見た目になっていない。
- [ ] 狭いウィンドウでパネルを折りたためる。
- [ ] Pointer Eventsを前提に将来タッチ対応を阻害していない。

### セキュリティ

- [ ] 外部APIなし。
- [ ] 画像外部送信なし。
- [ ] CDNなし。
- [ ] Tauri権限最小化。
- [ ] shell権限なし。
- [ ] Python sidecarは許可したコマンドだけ実行する。
- [ ] Python CLIで任意shell文字列を受け付けない。
- [ ] stdoutへ機密データ・巨大画像データを出していない。
- [ ] CSP設定済み。
- [ ] ログへ画像Data URLを出していない。
- [ ] 元画像を上書きしていない。

### テスト

- [ ] 起動テスト。
- [ ] 基本フロー。
- [ ] Undo/Redo。
- [ ] 自動保存復旧。
- [ ] 日本語IME。
- [ ] 異常系。
- [ ] 書出し解像度。
- [ ] 連続編集時メモリ。
- [ ] Windows本番ビルド。
- [ ] Python単体テスト。
- [ ] Python sidecar呼び出しテスト。
- [ ] 画像差分レポート生成。
- [ ] ログ縮約レポート生成。

### 納品

- [ ] アプリ本体。
- [ ] ソースコード。
- [ ] USER_GUIDE。
- [ ] FINAL_REPORT。
- [ ] STATUS.md最終更新。

---

# 12. Codex向け初回指示

この設計書を読み込ませた後、最初のCodex指示として以下を使用する。

```text
この設計書に基づいて Fukidashi Studio を実装してください。

最初に設計書を通読し、以下だけを出してください。
1. 要件の理解を10項目以内で要約
2. 技術的なリスク
3. CP1〜CP10の実装順序と依存関係
4. 最初に確認が必要な事項
5. 着手後に自走できる範囲

この段階ではまだ大規模実装を開始しないでください。
承認後はCP単位で実装・テスト・STATUS.md更新・git commitまで進めてください。

重要:
- PC版を先に完成させる。
- ただしUI、EditorEngine、ProjectRepository、PlatformFileServiceを分離し、将来のスマホ対応を妨げない。
- 画像は外部送信しない。
- 元画像を上書きしない。
- UIは古い業務ソフト風にしない。
- 書出し解像度と画面表示倍率を混同しない。
- 吹き出しと文字は別オブジェクトとして保持し、pairIdで関連付ける。
- 吹き出し拡大時に文字サイズまで自動拡大しない。
- 日本語IMEを実画像・実テキストで必ずテストする。
- 仕様外の大規模追加は行わない。
- Pythonは自由に使用してよい。特に大量ファイル走査、画像検査、画像差分、ログ集約、fixture生成、レポート生成はPythonを優先する。
- 決定論的に処理できる大量作業は、Codexが個別に読み込む前にPythonで縮約する。
- 反復作業を2回以上行う見込みがある場合、Pythonスクリプト化・テスト化・自動化を検討する。
- Pythonツールは巨大なstdoutを返さず、要約JSONを返す。raw detailはファイル保存し、そのパスだけを返す。
- Python利用はトークン節約だけを理由に品質を落とさない。最終判断が必要な箇所はCodexが確認する。
```

---

# 13. 将来のスマホ対応時の設計メモ

MVP実装時から以下を守る。

### UI

- 右Inspectorを固定幅前提にしすぎない。
- `InspectorContent` と表示コンテナを分離する。
- DesktopではSide Panel、MobileではBottom Sheetとして同じ内容を再利用する。
- Asset PanelもDrawer化可能にする。
- 主要ツールはアイコン + tooltip + accessible label。
- pointer inputを使用する。
- hoverだけで意味が伝わるUIにしない。

### Canvas

- pinch zoomの追加余地を残す。
- two-finger panの追加余地を残す。
- 44px程度のタッチターゲットを意識する。
- 小画面では選択ハンドルを大きくできる設定を持たせる。

### File I/O

- `PlatformFileService` をインターフェース化する。
- Androidのcontent URI、iOSのfile URI差異をUI層へ漏らさない。
- プロジェクトの内部Asset IDとOSパスを分離する。

### 性能

- 4K画像を常時フル解像度で再描画し続けない設計を検討可能にする。
- プレビュー用画像とエクスポート用原寸データを分離できるようAssetモデルを設計する。
- 大画像や多数レイヤー時はキャッシュ戦略を追加できるようにする。

---

# 14. 設計判断ログ（初版）

| 判断 | 採用 | 理由 |
|---|---|---|
| デスクトップ方式 | Tauri 2 | PC先行と将来モバイル対応の両立を狙う |
| UI | React + TypeScript | 編集状態の多いGUIを部品化しやすい |
| Canvas | Fabric.js | 画像、テキスト、選択、変形、シリアライズ機能を利用できる |
| 保存 | appData内の内部プロジェクト + autosave | MVPで作業喪失防止を優先 |
| プロジェクト配布形式 | 後回し | ZIP等の独自形式でMVPを遅らせない |
| 縦書き | v1.1候補 | 日本語漫画で重要だが実装論点が多いため、まず横書きMVPを完成させる |
| 外部API | なし | ローカル画像編集に不要 |
| テンプレ形式 | PNG / WebP優先 | 透明画像を安全・単純に扱う。SVGは安全化検討後 |
| 吹き出しと文字 | 別オブジェクト + pairId | 吹き出しサイズと文字サイズを独立調整するため |
| Python | 補助エンジン + 開発自動化 + token-saving layer | 決定論的な大量処理をLLMコンテキストから外すため |
| Python通信 | 単発CLI + compact JSON優先 | 常駐サーバーの複雑性をMVPへ持ち込まないため |
| Python配布 | PyInstaller sidecar候補 | PC利用者側のPython導入負担を避けるため |

---

# 15. 参考にする公式一次情報

実装時は必ず公式一次情報を再確認する。

- Tauri 2: https://v2.tauri.app/
- Tauri prerequisites: https://v2.tauri.app/start/prerequisites/
- Tauri dialog plugin: https://v2.tauri.app/plugin/dialog/
- Fabric.js: https://fabricjs.com/
- Fabric.js Canvas API: https://fabricjs.com/api/classes/canvas/
- Fabric.js Textbox API: https://fabricjs.com/api/classes/textbox/
- Vite Guide: https://vite.dev/guide/
- React: https://react.dev/
- Tauri Sidecar: https://v2.tauri.app/develop/sidecar/
- PyInstaller: https://pyinstaller.org/
- Pillow: https://pillow.readthedocs.io/

---

## 変更履歴

- v1.3（2026-07-09）: 可変画像サイズ対応版。852×1280px を含む複数サイズを自然に扱えるよう、内部論理キャンバスを元画像サイズへ一致させる方針、特定サイズ固定禁止、Fit表示、可変サイズ向けテスト要件、サイズプリセット方針を追加。
- v1.2（2026-07-09）: GUI強化版。左操作 / 右プレビューの標準レイアウト、設定による左右反転、下部プロパティの即時プレビュー、文字の縦 / 横切替（標準は縦）、文字縁取り、スポイト、四角・丸などの図形ツール、Recent Assets（過去素材呼び出し）を追加。
- v1.1（2026-07-09）: Python統合方針を追加。Pythonを画像補助処理・検証・テスト自動化だけでなく、Codexのトークン節約を目的としたScript-first layerとして定義。大量ログ・画像・プロジェクト走査をPythonで縮約し、要約JSONだけをLLMへ返す運用、Tauri sidecar / PyInstaller候補、Python CLI契約、テスト・配布方針を追加。
- v1.0（2026-07-09）: 初版。Windows PC先行、将来モバイル対応を前提に、Tauri 2 + React + TypeScript + Fabric.js構成を採用。吹き出しテンプレ管理、テキスト編集、レイヤー、Undo/Redo、自動保存、PNG書出し、UI/UX、テスト、セキュリティ、Codex初回指示を定義。
